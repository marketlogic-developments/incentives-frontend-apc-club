services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8050:8050"
    environment:
      NODE_ENV: production
    volumes:
      - /app/node_modules
    command: >
      npm start